# 2026-08-20 — Gate 2 implementation handoff

## Goal

Begin Gate 2 only after freezing its scientific design, then prepare a small local validation that is safe for the base-model Apple Silicon MacBook Air.

## Protocol frozen before implementation

`docs/gate-2-protocol.md` fixes the data seeds, transition definition, spatial residual target, action inputs, baselines, model seeds, balanced loss, held-out metrics, counterfactual retrieval set, stress slices, and pass/borderline/fail thresholds.

The protocol commit preceded all Gate 2 predictor code. Gate 1 remains complete and frozen.

## Work completed

- Added `src/latent_stroke_dynamics/gate2.py`.
  - Deterministic one-stroke transitions.
  - Stable split fingerprints.
  - Endpoint-reversal-invariant seven-value stroke encoding.
  - Fractional patch coverage and patch-center coordinates.
  - True, shifted, width-changed, and intensity-changed counterfactuals.
  - Identity, training-mean, shared linear, and shared MLP residual predictors.
  - Balanced action/outside loss and held-out spatial metrics.
  - Counterfactual retrieval over the union of candidate action regions.
- Added `tests/test_gate2.py` for deterministic logic and tensor behavior.
- Added `experiments/02_one_step_prediction.py`.
  - Independent train, validation, test, and optional stress generation.
  - Chunked frozen DINOv2 encoding.
  - Float16 disk feature caches and float32 active batches.
  - Encoder unloading before predictor training.
  - Tiny-set overfit diagnostic.
  - Validation-based choice between linear and MLP families.
  - Test error, crowding diagnostics, and counterfactual retrieval.
  - Formal-eligibility guard so smoke runs cannot declare a pass.

## Local unit-test result

Navid ran the complete suite on the base-model MacBook Air using Python 3.14.4 and pytest 9.1.1:

```text
collected 14 items
tests/test_gate1.py ....
tests/test_gate2.py .......
tests/test_renderer.py ...
14 passed in 1.91s
```

This validates the deterministic core logic in the actual local environment. It does not yet validate the end-to-end encoder, cache, training, or artifact path.

## M1 smoke command

Run:

```bash
python experiments/02_one_step_prediction.py \
  --train-samples 64 \
  --val-samples 16 \
  --test-samples 32 \
  --model-seeds 11 \
  --epochs 8 \
  --patience 3 \
  --encode-batch-size 8 \
  --encode-chunk-size 32 \
  --train-batch-size 8 \
  --encoder-device cpu \
  --train-device cpu \
  --output-dir outputs/gate2-smoke
```

On an identical retry after features have already been encoded, append `--reuse-cache` so the frozen features are loaded from `outputs/gate2-smoke/cache/`.

## Outputs to inspect before any formal run

1. The complete smoke terminal output.
2. `overfit_check.json` — the loss must decrease; the amount is diagnostic.
3. `training_history.csv` — check for finite, improving validation loss.
4. `gate_diagnostics.csv` — status must be `diagnostic_only`; inspect baseline margins and retrieval rather than treating it as a decision.
5. `aggregate_metrics.csv` and `counterfactual_retrieval.csv`.
6. `baseline_improvement.png`.
7. `example_residual_prediction.png`.
8. Any traceback, memory pressure, or unexpectedly long stage.

Do not run the formal 1,000/200/300, three-seed, three-stress-slice configuration until this smoke handoff has been reviewed.
