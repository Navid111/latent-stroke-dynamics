# 2026-08-20 — Gate 2 implementation and development review

## Protocol and implementation

Gate 2 was frozen before predictor code. The implementation added deterministic transitions, normalized action inputs, spatial patch-token residual predictors, trivial baselines, balanced loss, counterfactual retrieval, M1-safe caching, tests, tables, and plots.

The first local suite passed 14 tests. Engineering smoke 1 completed, reduced the tiny-overfit loss by 98.14%, and exposed two integrity issues: duplicate retrieval outcomes and accidental exposure of formal-seed prefixes. Both were repaired transparently before any formal run. Untouched amended formal data seeds are `20260824`–`20260827`.

## Revised local tests

After the uniqueness repair, Navid ran:

```text
collected 15 items
15 passed in 1.99s
```

## Development v2

The 256/64/96-example, seed-11 CPU run completed on the base-model M1. All candidate-uniqueness and finite-metric checks passed. Encoding, caching, optimization, evaluation, and artifact generation completed without a reported resource failure.

Validation selected the linear predictor. Test action-region MSE was `0.000973`, compared with `0.002270` for identity and `0.002003` for mean delta. This is a 57.2% improvement over identity and 51.4% over mean delta.

Crowding-specific linear improvements were:

- 0 prior strokes: +77.3%;
- 5 prior strokes: +35.2%;
- 15 prior strokes: +9.3%.

All were positive. The linear validation curve flattened around epoch 27 and remained better than the MLP.

Counterfactual retrieval was 22/96, or 22.9%, below the 25% random-choice reference and far below the frozen 50% requirement. The shared-scale heatmap showed a smooth, lower-amplitude predicted residual with its largest error near the proposed stroke. This supports a regression-to-the-average interpretation: broad consequences are predictable, but exact action details are not yet reliably recovered.

The development run remains `diagnostic_only`. It is neither a Gate 2 pass nor a formal fail.

## Next step

A new no-retraining diagnostic reads the existing retrieval and split-metadata CSV files. It reports candidate-selection preferences, pairwise true-outcome wins, Wilson intervals, margins, and retrieval by crowding, width, intensity, and length.

Run after pulling and passing tests:

```bash
python experiments/02b_retrieval_diagnostics.py \
  --input-dir outputs/gate2-dev-v2
```

This command does not load DINOv2, retrain a predictor, alter an output cache, or access formal data.
