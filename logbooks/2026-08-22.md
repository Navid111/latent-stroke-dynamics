# 2026-08-22 — Checkpoint success and all-method smoke handoff

## Checkpoint result

The repository suite passed all 33 tests. The separate Stage 3 demonstration MLP trained in 48.95 seconds on CPU and saved an integrity-checked 833-parameter checkpoint.

Validation loss reached its minimum of `0.000164624` at epoch 29 and increased mildly at epoch 30. The trainer correctly restored the epoch-29 state. There was no train/validation overlap, no paired test data were generated or used, and the reloaded checkpoint reproduced predictions exactly.

## All-method smoke implementation

Added the development-only random/exact/learned planner smoke. It loads and validates the local checkpoint, checks its exact metadata and digest, and then runs all three methods with 20 strokes and 32 candidates per step on the fixed synthetic target.

The learned method ranks predicted next canvases but executes and observes the selected action with the exact renderer. It records exact rank, top-1/top-5 agreement, one-step regret, improving-step rate, deterministic replay, and runtime. All methods save progress CSVs, stroke JSON, final canvases, GIFs, fixed-scale error panels, and a shared MSE curve.

The smoke is ready for local execution before freezing the six-target controlled command.
