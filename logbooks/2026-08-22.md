# 2026-08-22 — All-method smoke pass and controlled freeze

## All-method smoke

The saved 833-parameter MLP successfully drove the sequential painter. After 20 actions on the development target, learned final MSE was `0.089196`, compared with `0.144913` for random and `0.074427` for exact greedy. Learned planning reduced error by 40.32%, improved 19 of 20 steps, and remained 19.84% above exact.

Exact top-1 agreement was 55% and top-5 agreement was 80%. Mean exact rank was 4.6 of 32 and mean regret was `0.000653`. One rank-25 error at step 7 briefly worsened the canvas, but replanning recovered. Deterministic replay passed. Visual results and fixed-scale error panels agreed with the metrics.

## Controlled configuration

The six-target Stage 3 comparison was frozen before controlled data generation. It uses 100 actions, 128 candidates per step, six fixed target/planner seed pairs, the validated local checkpoint, and the committed success criteria.

The controlled runner delegates each target to the already validated all-method path, checks every subrun, aggregates metrics and learned diagnostics, produces cross-target progress and montage figures, and applies the frozen conjunctive decision. A completed result cannot be overwritten; incomplete work is kept separately for diagnosis.

## Next checkpoint

Run `experiments/07_pixel_planner_controlled.py --validate-only`. This must report that validation passed and no controlled data were generated before the single controlled command is authorized.
