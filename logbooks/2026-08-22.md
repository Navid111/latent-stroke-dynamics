# 2026-08-22 — Stage 3 success, qualitative limits, and representation extension

## Completed core

The controlled Stage 3 run passed all frozen criteria. Learned planning reduced mean final MSE by 61.51% versus random and finished 18.93% above exact greedy. The painter, polarity repair, and best-frame output were validated.

## Qualitative deployment result

On normalized MNIST-3 at 100 strokes, exact greedy reached final MSE `0.022196` and best step 99. Learned pixel planning reached `0.040860` and best step 33. Improving exact candidates remained available, localizing the dominant late-stage limitation to learned ranking under crowded out-of-distribution states.

## Representation-extension freeze and foundation

Before new extension data, froze exactly two representations: deterministic unmasked frozen `facebook/vit-mae-base` spatial tokens and a small reconstruction-trained 32×16×16 autoencoder latent. Historical DINOv2 and pixels remain anchors only.

Navid validated all 46 foundation tests. ViT-MAE produced `[2, 196, 768]` frozen features, exact repeated-encoding difference `0.0`, and a positive fixed-stroke signal. The checkpoint's unused decoder-key report was expected because only its encoder is loaded.

## Development-smoke implementation

Added deterministic three-seed autoencoder training, validation-only selection, train-only latent standardization, reconstruction baseline, model hashing, and exact checkpoint reload checks. Added generic patch-dynamics training/evaluation utilities and four more tests.

Added one fixed development runner for both new representations. It uses only seeds `20261020`–`20261022`, trains the frozen linear/MLP families and seeds, evaluates four-way action retrieval and factor confusions, verifies exact-target oracle retrieval and encoded uniqueness, and atomically saves actual JSON/CSV artifacts. It cannot classify either representation or generate primary/stress data.

Next: `50 passed`, followed by the single development smoke. Review its saved `smoke_summary.json` before freezing any full command.
