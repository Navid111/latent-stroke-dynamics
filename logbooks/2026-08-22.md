# 2026-08-22 — Stage 3, qualitative deployment, and representation extension

## Completed core

Controlled Stage 3 passed all frozen criteria. The painter and best-frame output were validated. The exact MNIST diagnostic localized long-horizon learned degradation to candidate ranking under crowded out-of-distribution states.

## Representation development

The development smoke completed with all implementation checks passing and no primary/stress data. Task-autoencoder dynamics improved action-region MSE by 71.01% versus identity and reached 46.35% retrieval. ViT-MAE improved by 23.97% and reached 7.29% retrieval. Development metrics changed no scientific setting.

## Full-command validation and run

Navid validated 51 tests and `full_command_valid`. The one frozen full command was then executed once on untouched primary and stress seeds. It completed in 2,353.79 seconds. It must not be rerun or retuned.

The selected task autoencoder was seed 101 at epoch 50. It improved validation reconstruction MSE by 95.47% versus the train-mean image baseline, was non-collapsed, reloaded exactly, and passed protocol eligibility.

Task-autoencoder MLP dynamics improved action-region MSE by 70.65% versus identity and 68.62% versus mean delta. Four-way retrieval was 37.89%; position/width/intensity pairwise wins were 99.11%/68.89%/52.00%. All primary and secondary crowding improvements were positive.

ViT-MAE MLP dynamics improved by 33.08% versus identity and 30.63% versus mean delta. Four-way retrieval was 7.11%; pairwise wins were 87.00%/25.00%/36.22%. Crowding-60 stress performance was 13.69% worse than identity.

## Raw runner reporting mismatch

The raw runner marked task integrity false because separately batched true-candidate encodings differed by `1.7404556274414062e-05`, despite 100% oracle retrieval and unique encoded candidates. The written protocol requires 100% oracle retrieval and uniqueness, not bit equality. The raw summary remains unchanged.

The runner also assigned ViT-MAE the average-predictable label despite Section 9's rule that retrieval at or below 35% is not predictively usable.

Added a pure JSON-only adjudicator and tests. It performs no training, model loading, data generation, evaluation, or metric recomputation. Pending local validation, the written-protocol outcomes are task autoencoder average-predictable but not action-usable and ViT-MAE not predictively usable.
