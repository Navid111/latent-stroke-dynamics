# 2026-08-22 — Stage 3 success, qualitative limits, and extension foundation

## Completed core

The six-target controlled Stage 3 run passed all frozen criteria. Learned planning reduced mean final MSE by 61.51% versus random and finished 18.93% above exact greedy. The user-facing painter, polarity repair, and best-painting output were subsequently validated by 38 passing tests.

## Qualitative deployment result

On normalized MNIST-3 at 100 strokes, exact greedy reached final MSE `0.022196` with 88 improving actions and best step 99. Learned pixel planning reached `0.040860` with 42 improving actions and best step 33. The exact result localizes the dominant late-stage limitation to learned ranking in crowded out-of-distribution trajectories.

## Representation-extension freeze

Before implementing or generating new extension data, froze exactly two new representations: deterministic unmasked frozen `facebook/vit-mae-base` spatial tokens and a small reconstruction-trained convolutional autoencoder with a 32×16×16 latent. New primary seeds are `20261024`–`20261026`, stress seeds are `20261027`–`20261030`, and development seeds are `20261020`–`20261022`. Historical DINOv2 and pixel outcomes remain anchors only.

## Foundation implementation

Added a deterministic ViT-MAE wrapper that sets mask ratio to zero, supplies fixed ordered masking noise, restores raster patch order, checks that no patch was masked, normalizes tokens, and freezes all parameters. Added the frozen autoencoder architecture, train-only latent standardization, reconstruction metrics, freeze helper, and config-drift/seed-overlap validation.

Eight unit tests avoid network access and cover the configuration, deterministic MAE helpers, patch unshuffling, autoencoder shapes, latent standardization, freezing, and reconstruction metrics. A separate two-image MAE smoke may download weights and verifies `[2, 196, 768]` features, repeatability within `1e-7`, positive stroke sensitivity, and frozen weights without generating extension splits.

Next: local `46 passed`, validation-only output, and deterministic ViT-MAE smoke. No full extension run exists yet.
