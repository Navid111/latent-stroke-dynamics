# Logbook — 2026-08-19

## Environment

- Machine: base-model Apple Silicon MacBook Air M1
- Shell: macOS terminal
- Virtual environment: `.venv`
- Observed Python path: `.venv/lib/python3.14/...`
- Requested device: CPU

## Attempt

The Gate 1 smoke command reached `FrozenVisionEncoder` initialization but stopped before loading the pretrained model or encoding any canvases.

```bash
python experiments/01_embedding_sensitivity.py \
  --samples 3 \
  --crowding 0 5 \
  --batch-size 4 \
  --device cpu \
  --output-dir outputs/gate1-smoke
```

## Failure

`transformers.AutoImageProcessor` raised an `ImportError` because `torchvision` was not installed. The project's dependency list included `torch` but had accidentally omitted `torchvision`.

This is an environment/dependency failure, not an experimental result. Gate 1 remains untested.

## Repository fix

Added `torchvision>=0.17` to `pyproject.toml` so a normal editable install includes the image-processing dependency.

## Next command sequence

```bash
git pull
python -m pip install -e ".[dev]"
python -c "import torch, torchvision; print('torch', torch.__version__, 'torchvision', torchvision.__version__)"
pytest
python experiments/01_embedding_sensitivity.py \
  --samples 3 \
  --crowding 0 5 \
  --batch-size 4 \
  --device cpu \
  --output-dir outputs/gate1-smoke
```

If `torchvision` has no compatible wheel or import fails under Python 3.14, recreate the environment with Python 3.12 rather than debugging an unstable package combination.
