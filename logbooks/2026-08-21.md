# 2026-08-21 — Gate 2 formal decision

## Pre-formal diagnosis

The no-retraining development decomposition passed as part of an 18-test suite and identified width discrimination as the main retrieval bottleneck. Position and intensity pairwise wins were each 66.7%, while width pairwise wins were 46.9%. No implementation defect justified changing the frozen primary experiment.

## Formal run

The preregistered 1,000/200/300 run completed with three 100-example stress slices and model seeds `11`, `22`, and `33`. The configuration matched the frozen command:

```text
formal_run_requested: true
formal_eligible: true
```

Validation selected the MLP family. All implementation sanity, finite-metric, candidate-uniqueness, and seed-stability checks passed.

## Formal result

- selected MLP action-region MSE: 0.000860;
- identity action-region MSE: 0.002250;
- mean-delta action-region MSE: 0.002003;
- improvement versus identity: 61.8%;
- improvement versus mean delta: 57.1%;
- crowding improvements: +79.0%, +43.3%, and +25.0%;
- counterfactual retrieval: 27.7%.

The model passed both average-error margins, all crowding checks, and all sanity checks. It failed the required 50% counterfactual retrieval criterion. Since the frozen protocol defines retrieval at or below 35% as fail, the formal Gate 2 decision is **fail**, not borderline.

## Secondary evidence

The selected MLP reduced average action-region error strongly on unseen width, unseen intensity, and unseen crowding slices. More training data improved average prediction and selected the nonlinear family, but exact action discrimination remained close to chance.

## Decision and next step

Do not rerun the formal experiment or tune against the formal test set. Preserve the mixed result: latent consequences are predictable on average but not precise enough for action-level planning with the current objective and architecture.

Next, complete a no-retraining formal retrieval decomposition and implement the already-required pixel-space control. Gate 3 planning remains blocked.
