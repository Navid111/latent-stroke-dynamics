# 2026-08-23 — Latent-space planner

## Protocol freeze

Started the day by freezing the post-formal latent-space planner before implementation or planner data. The fixed methods are random, exact pixel, learned pixel, three-seed MSE-only latent, and three-seed ranking-aware latent. Latent scores are full-grid MSE between L2-normalized predicted-next and target patch features, averaged across seeds 11, 22, and 33.

## Foundation and hash freeze

The full suite passed 79 tests. Validation-only checks established frozen models, deterministic encoding/scoring, finite expected shapes, no data generation, and no training. Six latent predictor state hashes were frozen. A one-character pixel-digest transcription error was corrected using the internally matching checkpoint payload and recomputed state hash; no weight or result changed.

## Smoke implementation and validation

The guarded runner encoded the target once, re-encoded the exact observed canvas each step, used fixed three-seed means, executed selected strokes exactly, saved best/final states, and added rank, regret, and Spearman diagnostics. The full suite passed 84 tests. Unauthorized validation loaded no models, generated no target, and created no output.

## Smoke execution

The one separately authorized smoke completed with status `latent_planner_smoke_complete_integrity_passed`. All hashes, state-transition checks, deterministic replays, no-training checks, and frozen settings passed.

Final MSE was 0.138367 random, 0.084314 exact pixel, 0.092628 learned pixel, 0.095354 latent MSE, and 0.116399 latent ranking. Latent MSE achieved 90% exact top-5, mean rank 3.95, and mean score-to-exact Spearman 0.622; latent ranking achieved 45% top-5, mean rank 7.05, and Spearman 0.414. Ranking-aware one-step retrieval superiority therefore did not produce the better short planner on this diagnostic target.

The smoke is now closed and cannot select settings or be rerun. Controlled planning remains unauthorized. The next permitted work is guarded controlled-runner implementation and no-data validation.
