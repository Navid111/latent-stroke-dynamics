# 2026-08-23 — Latent-space planner

The post-formal planner protocol, six latent hashes, smoke, and controlled comparison were completed in one guarded workflow. Foundation validation and all runner tests passed before data. The smoke completed with integrity and indicated latent MSE might outperform latent ranking.

The separately authorized controlled comparison then evaluated six untouched targets, five methods, 100 steps, and 128 candidates. All targets completed and every integrity check passed. Mean final MSE was 0.146905 random, 0.045569 exact pixel, 0.053630 learned pixel, 0.076142 latent MSE, and 0.090934 latent ranking.

Latent ranking improved every target and beat random by 38.10%, but it was 1.996× exact-pixel error and therefore failed the frozen maximum ratio of 1.5×. It was also 1.194× latent-MSE error. Top-1/top-5 exact candidate rates were 5%/13% for ranking and 9.5%/27.2% for latent MSE. Mean score-to-exact Spearman was 0.426 versus 0.549.

This preserves the formal success while establishing its boundary: ranking improved four-way one-step retrieval but did not produce the stronger 128-candidate long-horizon planner. The controlled run is closed permanently. No rerun, retuning, or training on controlled targets is permitted.

## Planner-score alignment extension frozen

Read-only review separated score misalignment from forced-horizon overpainting. Ranking's mean final MSE was about 8.9% above its mean best MSE, but its oracle best-frame ratio to exact pixel was still about 1.832×. The weaker candidate-ordering diagnostics therefore motivate a narrow frozen-score audit before any new training.

A separate post-controlled protocol now reserves only new seeds. It will compare normalized latent MSE, normalized latent L1, pixel-error-weighted latent MSE, decoded pixel L1, and decoded pixel L1 plus quarter-weighted Sobel L1 across both frozen latent ensembles. A later phase will test the development winner with and without an untuned no-op action. Development and confirmatory data remain unauthorized.

## Guarded score-audit runner implemented

The full Stage A development runner is now present behind an authorization guard. It can build the independently defined blank/exact/random state bank, evaluate all ten frozen predictor/score pairs, save exact candidate labels and per-state diagnostics atomically, and apply only the preregistered lexicographic selection rule. Validation-only mode stops before model loading, target generation, state trajectories, candidate proposals, output creation, or training. Seven new tests cover configuration drift, closed-resource references, side-effect guards, prediction determinism, all five scores, and selection logic.

## Score-audit validation and authorization

Navid ran the complete validation on Apple Silicon with Python 3.14.4. All 96 tests passed in 4.27 seconds. The guard returned `planner_score_audit_runner_valid_unauthorized`; all six predictor hashes, the autoencoder hash, and latent-statistics hash references matched; no models or data were loaded or generated; and every historical experiment stayed closed. A separate commit then authorized exactly one development score audit while keeping model training, closed-target reuse, planner development, and confirmatory evaluation unauthorized.

## Score-audit completion

The single authorized score audit completed all eight targets and 72 candidate sets with implementation integrity passed. Every model stayed frozen, all exact candidate sets were unique, no closed target was reused, and no training or fine-tuning occurred. The preregistered primary selector chose the MSE-only ensemble with normalized-latent L1 score: mean exact regret 0.001052, top-5 rate 51.39%, mean rank 12.39, and Spearman 0.619. Relative to the same predictors scored by normalized-latent MSE, L1 reduced mean regret by about 28.8% and increased top-5 selection by 18.06 percentage points. The authorization is consumed, the audit is archived and closed, and planner development remains unauthorized.

## Guarded planner-development runner implemented

A separate runner now implements the next preregistered phase without authorizing data. It compares exact pixel, learned pixel, current normalized-latent MSE forced for 100 steps, the frozen normalized-latent L1 winner forced for 100 steps, and that L1 winner with the exact-current-state zero-margin no-op. It uses three new reserved targets, deterministic replays, exact rendering after every choice, observed-canvas re-encoding, atomic outputs, and a frozen confirmatory-eligibility decision. Seven new tests cover the archived selection, resource references, side-effect guards, exact L1 scoring, no-op semantics, deterministic planning, and eligibility logic.
