# Planner-score long-horizon development closed

The single authorized comparison completed with implementation integrity and deterministic replay checks passed. Its authorization is consumed.

Frozen outcome:

- forced normalized-latent L1 modestly improved over forced normalized-latent MSE;
- the zero-margin no-op stopped prematurely;
- the preregistered decision is `not_eligible`;
- confirmatory evaluation is unauthorized and must not run.

Do not rerun:

```bash
python experiments/19_planner_score_development.py --planner-development
```

Preserve the complete local output directory:

```text
outputs/planner-score-audit-planner-development-2026-08-23/
```

The compact immutable archive is under `results/planner-score-alignment/`, with interpretation in `docs/planner-score-development-results.md`.
